// Importa os matchers customizados do jest-dom (ex: toBeInTheDocument, toHaveTextContent).
// Este arquivo é carregado pelo Jest antes de cada suíte de testes (ver setupFilesAfterEnv no jest.config.ts).
import "@testing-library/jest-dom";
